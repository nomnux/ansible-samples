# Ansible AWS module samples

## Requirements

* Ansible
* Python boto, boto3
* AWS credentials
    * ~/.aws/config
    * ~/.aws/credentials

## Installation

```
$ pip install ansible boto boto3 [--user]
```
